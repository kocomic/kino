services:
  app:
    volumes:
      - type: bind
        source: "${KINO_DATA_PATH:?Set KINO_DATA_PATH to an existing absolute local NAS path}"
        target: /data
        bind:
          create_host_path: false
