# NAS

Follow [deployment](DEPLOYMENT.md). The published Compose template is
`compose.ghcr.yaml`; `compose.synology.yaml` is a local-build alternative.
Verify the NAS CPU architecture, actual volume paths and permissions before
deploying. Keep the data directory and administrator token private.
